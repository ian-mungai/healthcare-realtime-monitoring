[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
    "B",
    "UP"
]

[tool.pytest.ini_options]
testpaths = [
    "tests",
    "scripts/synthea-loader/tests"
]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
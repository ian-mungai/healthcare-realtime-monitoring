dataset: healthcare_realtime_athena/awsdatacatalog/healthcare_realtime_dbt/dim_encounter

checks:
  - row_count:
      threshold:
        must_be_greater_than: 0

columns:
  - name: encounter_key
    checks:
      - missing:
      - duplicate:

  - name: patient_key
    checks:
      - missing:

  - name: patient_id
    checks:
      - missing:

  - name: encounter_start_at
    checks:
      - missing:

  - name: encounter_end_at
    checks:
      - missing:

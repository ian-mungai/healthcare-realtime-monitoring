dataset: healthcare_realtime_athena/awsdatacatalog/healthcare_realtime_dbt/dim_date

checks:
  - row_count:
      threshold:
        must_be_greater_than: 0

columns:
  - name: date_key
    checks:
      - missing:
      - duplicate:

  - name: full_date
    checks:
      - missing:
      - duplicate:

  - name: calendar_year
    checks:
      - missing:

  - name: month_number
    checks:
      - missing:

  - name: day_of_month
    checks:
      - missing:

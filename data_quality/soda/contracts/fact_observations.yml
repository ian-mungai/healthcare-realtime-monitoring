dataset: healthcare_realtime_athena/awsdatacatalog/healthcare_realtime_dbt/fact_observations

checks:
  - row_count:
      threshold:
        must_be_greater_than: 0

columns:
  - name: observation_id
    checks:
      - missing:

  - name: patient_id
    checks:
      - missing:

  - name: loinc_code
    checks:
      - missing:

  - name: value
    checks:
      - missing:

  - name: effective_datetime
    checks:
      - missing:
